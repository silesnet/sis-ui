{{outlet}}
Is authenticated: {{ session.isAuthenticated }}<br />
{{#if session.isAuthenticated}}
    <button type="submit" {{action 'logOut'}}>Log Out</button>
    {{#if currentUser.user }}
      <p>Signed in as {{ currentUser.user.login }}, {{ currentUser.user.fullName }}</p>
    {{/if}}
{{/if}}
<button type="submit" {{action 'logIn'}}>Log In</button>
